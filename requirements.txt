# Conservative pins for corporate Windows VMs running Python 3.9.
# Newer cryptography wheels may fail to load the Rust native module on older
# Windows images ("DLL load failed while importing _rust").
paramiko==3.4.0
cryptography==3.3.2
bcrypt==3.2.2
pynacl==1.5.0
cffi==1.15.1
pycparser==2.22
tzdata
